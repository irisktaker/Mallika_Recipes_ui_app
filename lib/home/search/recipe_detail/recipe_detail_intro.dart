import 'package:flutter/material.dart';

class RecipeDetailInfo extends StatelessWidget {
  const RecipeDetailInfo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
